server {
    listen 80;
    listen [::]:80;
    root /usr/share/nginx/html/;
        index index.php index.html index.htm;
        charset utf-8;
        location / {
            try_files $uri $uri/ /index.html;
        }
}
